#include <stdio.h>

int main(void) {
 int n1, n2, s;
 printf("informe um número:");
 scanf("%i", &n1);
 printf("informe outro número:");
 scanf("%i", &n2);
 s = n1 + n2;
 printf("resultado: %i\n", s);
  return 0;
}
