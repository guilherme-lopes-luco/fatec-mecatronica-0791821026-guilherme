#include <stdio.h>

int main(void) {
float a,b,x,y;
printf("informe os valores de a,b e x;")
scanf("%f", &a);
scanf("%f%f%f", &a,&b, &x);
y = (a*x) +8
printf("resultado da equacao: %.3f/n", y);
  return 0;
}
